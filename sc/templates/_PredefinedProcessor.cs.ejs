namespace <%= namespace %>
{
    public class <%= name %> : <%= abstractProcessor.options["className"] %>
    {
        public static readonly <%= name %> Instance = new <%= name %>();
        public override void SafeProcessorExecute(<%= arguments.options["className"] %> args)
        {
            throw new System.NotImplementedException();
            return Task.CompletedTask;
        }

        public override bool SafeProcessorCondition(<%= arguments.options["className"] %> args)
        {
            var safeCondition = true;
            return safeCondition;
        }
    }
}