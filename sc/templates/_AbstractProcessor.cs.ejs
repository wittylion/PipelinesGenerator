using MyNamespace;

namespace MyNamespace
{
    public abstract class <%= name %>
    {
        public void Process(<%= argumentsClassName %> args)
        {
            if (args.Aborted)
            {
                return;
            }

            if (!this.SafeCondition(args))
            {
                return;
            }

            this.SafeProcess(args);
        }

        public abstract void SafeProcess(<%= argumentsClassName %> args);

        public virtual bool SafeCondition(<%= argumentsClassName %> args)
        {
            return true;
        }
    }
}
