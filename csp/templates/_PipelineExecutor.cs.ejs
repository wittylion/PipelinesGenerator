using System.Threading.Tasks;
using Pipelines;

namespace <%= namespace %>
{
    public class <%= name %> : PipelineExecutor
    {
        public static readonly <%= name %> Instance = new <%= name %>(<%= pipelineClassName %>.Instance);

        public <%= name %>(IPipeline pipeline) : base(pipeline)
        {
        }
    }
}