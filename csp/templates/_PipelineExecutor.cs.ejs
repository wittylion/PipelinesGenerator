using System.Threading.Tasks;
using Pipelines;

namespace <%= namespace %>
{
    public class <%= name %>
    {
        public static readonly <%= name %> Instance = new <%= name %>(<%= pipelineClassName %>.Instance);

        public IPipeline Pipeline { get; }

        public <%= name %>(IPipeline pipeline)
        {
            this.Pipeline = pipeline;
        }

        public virtual Task Execute(<%= argumentsClassName %> arguments)
        {
            PipelineRunner runner = new PipelineRunner();
            return runner.RunPipeline(this.Pipeline, arguments);
        }
    }
}