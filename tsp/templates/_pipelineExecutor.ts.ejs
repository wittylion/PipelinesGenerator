import { IPipeline, PipelineRunner, PipelineExecutor } from "solid-pipelines";
import { <%= argumentsClassName %> } from './<%= argumentsFileName %>'
import { <%= pipelineClassName %> } from './<%= pipelineFileName %>'

export class <%= name %> extends PipelineExecutor {
    public static Instance: <%= name %> = new <%= name %>(<%= pipelineClassName %>.Instance);

    constructor(
        public pipeline: IPipeline,
        public pipelineRunner: PipelineRunner = PipelineRunner.StaticInstance,
    ) {
        super(pipeline, pipelineRunner);
    }
}