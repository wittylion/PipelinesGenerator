namespace <%= namespace %>
{
    public class <%= name %>
    {
        public bool IsAborted { get; set; }

        <% argumentsMemebers.forEach(function(member){ %>public string <%= member %> { get; set; }
        <% }); %>
    }
}