import * as Processors from './processors'
import { <%= abstractProcessorClassName %> } from './<%= abstractProcessorClassName %>'

export class <%= name %> {
    public static readonly Instance = new <%= name %>();

    GetProcessors(): <%= abstractProcessorClassName %>[] {
        return [<% processors.forEach(function(processor){ %>
            this.Get<%= processor.options["className"] %>Instance(),<% });%>
        ];
    }<% processors.forEach(function(processor){ %>
    
    public Get<%= processor.options["className"] %>Instance(): <%= abstractProcessorClassName %> {
        return Processors.<%= processor.options["className"] %>.Instance;
    }<% });%>
}