import { IPipeline, PipelineRunner } from "solid-pipelines";
import { <%= argumentsClassName %> } from './<%= argumentsFileName %>'
import { <%= pipelineClassName %> } from './<%= pipelineFileName %>'

export class <%= name %> {
    public static Instance: <%= name %> = new <%= name %>(<%= pipelineClassName %>.Instance);

    constructor(public pipeline: IPipeline) {
    }

    execute(args: <%= argumentsClassName %>) : Promise<void> {
        var runner:PipelineRunner = new PipelineRunner();

        return runner.RunPipeline(this.pipeline, args);
    }
}