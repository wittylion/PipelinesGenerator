import { SafeProcessor } from "solid-pipelines";
import { <%= pipelineName %>Arguments } from "./<%= pipelineName %>Arguments";

export abstract class <%= pipelineName %>Processor extends SafeProcessor<<%= pipelineName %>Arguments> {  }
