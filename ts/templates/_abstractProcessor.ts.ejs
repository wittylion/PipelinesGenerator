import { SafeProcessor } from "solid-pipelines";
import { <%= argumentsClassName %> } from "./<%= argumentsFileName %>";

export abstract class <%= pipelineName %> extends SafeProcessor<<%= argumentsClassName %>> {  }
