const GENERATE_ARGUMENTS_FILE = {
    PROCESSOR: Symbol.for("Generate arguments file processor"),
    PIPELINE: Symbol.for("Generate arguments file pipeline"),
    EXECUTOR: Symbol.for("Generate arguments file executor"),
}

export default GENERATE_ARGUMENTS_FILE
