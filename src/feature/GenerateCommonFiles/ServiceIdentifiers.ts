const GENERATE_COMMON_FILES = {
    PROCESSOR: Symbol.for("Generate common files processor"),
    PIPELINE: Symbol.for("Generate common files pipeline"),
    EXECUTOR: Symbol.for("Generate common files executor"),
    MODELS_PROVIDER: Symbol.for("Models provider"),
    GENERATORS_PROVIDER: Symbol.for("Generators provider"),
}

export default GENERATE_COMMON_FILES
