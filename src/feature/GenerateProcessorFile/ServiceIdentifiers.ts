const GENERATE_PROCESSOR_FILE = {
    PROCESSOR: Symbol.for("Generate processor file processor"),
    PIPELINE: Symbol.for("Generate processor file pipeline"),
    EXECUTOR: Symbol.for("Generate processor file executor"),
}

export default GENERATE_PROCESSOR_FILE
